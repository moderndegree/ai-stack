# Copy to .env and fill in values
# Generate random passwords with: node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"

# ── Anthropic ─────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── Core infrastructure ───────────────────────────────────────────────────────
POSTGRES_PASSWORD=
MCP_POSTGRES_PASSWORD=     # password for the read-only MCP postgres user
REDIS_PASSWORD=
N8N_ENCRYPTION_KEY=        # 32-char random string
SEARXNG_SECRET_KEY=        # 64-char hex string

# ── LangFuse — internal service credentials ───────────────────────────────────
LANGFUSE_DB_PASSWORD=
LANGFUSE_NEXTAUTH_SECRET=
LANGFUSE_SALT=
LANGFUSE_ENCRYPTION_KEY=   # exactly 64 hex characters

# External URL for LangFuse (used in NEXTAUTH_URL — required for login to work)
LANGFUSE_URL=https://langfuse.example.com

# ── LangFuse — API keys (create project in UI at :3004, then paste here) ──────
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=

# ── Browserless ───────────────────────────────────────────────────────────────
BROWSERLESS_TOKEN=

# ── Gitea ─────────────────────────────────────────────────────────────────────
# External URL for Gitea (used in ROOT_URL — sets clone URLs and webhook targets)
GITEA_ROOT_URL=https://git.example.com
# Token created in UI at :3003 → Settings → Applications
GITEA_TOKEN=

# ── Garage S3 (run `bash garage/init.sh` after first start) ───────────────────
GARAGE_ACCESS_KEY_ID=
GARAGE_SECRET_ACCESS_KEY=

# ── claude-worker ─────────────────────────────────────────────────────────────
CLAUDE_WORKER_TOKEN=       # bearer token required on all API requests to :3002

# ── OpenCode ──────────────────────────────────────────────────────────────────
OPENCODE_SERVER_PASSWORD=  # password for HTTP basic auth on OpenCode server

# ── Optional claude-worker overrides ──────────────────────────────────────────
# DEFAULT_MAX_ITERATIONS=20
# DEFAULT_COMPLETION_PROMISE=RALPH_COMPLETE
# CLAUDE_TIMEOUT_MS=600000
